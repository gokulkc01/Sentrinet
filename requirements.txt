# SentryNet runtime dependencies (pinned to the versions in sentrinet_env).
# For CUDA torch, install the matching wheel from https://pytorch.org instead
# of the CPU/default torch line below.

numpy==2.4.2
scipy==1.17.1
pandas==3.0.1
matplotlib==3.10.8

torch==2.10.0

pettingzoo==1.25.0
gymnasium==1.2.3
pybullet==3.2.7
pygame==2.6.1

# Optional: experiment logging (training runs without it via try/except).
# wandb
